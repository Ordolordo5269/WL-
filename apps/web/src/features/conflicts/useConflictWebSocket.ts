// Stub — WebSocket handler removed in PR #32 (ACLED migration).
// Kept as no-op so ConflictTracker.tsx (UCDP) still compiles.
export function useConflictWebSocket(_onMessage: () => void) {
  // no-op
}
