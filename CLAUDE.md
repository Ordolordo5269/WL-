# Project Rules

## Database
- NEVER modify, delete, or alter the database (schema, data, migrations) without explicit user permission.
- Always ask before running any command that could affect the database.

## Commands
- Shell commands (build, test, lint, dev servers, git, npm, etc.) are authorized.
